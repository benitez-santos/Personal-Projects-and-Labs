The following is an update version of a Client-Server using Stream Socket Programming with better programming techniques.

To clean:
    make clean

To compile:
    make

To run the client:
    ftpc <remote-IP> <remote-port> <local-file-to-transfer>
    
To run the server:
    ftps <remote-port>
